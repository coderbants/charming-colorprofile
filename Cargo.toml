[package]
name = "rusty-colorprofile"
exclude = ["POLICIES.md"]
version = "0.4.3"
edition = "2021"
description = "Cleanroom Rust port of Charmbracelet's colorprofile v0.4.3 (color downsampling based on output, environment, and terminfo)"
license = "MIT"

[dependencies]
rusty-x-ansi = { version = "0.11.7", path = "../rusty-x-ansi" }
